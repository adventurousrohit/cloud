/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the "experimental" key
  //   reactStrictMode: true, // Enable React strict mode
  //   pageExtensions: ["page.jsx", "page.js", "api.ts", "api.js"], // Customize file extensions if needed
};

export default nextConfig;
